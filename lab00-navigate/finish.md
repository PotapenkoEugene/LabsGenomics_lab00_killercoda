# Well done

## What you practiced

| Command / key | Purpose |
|---------------|---------|
| `cd path` | Change into a directory |
| `cd /absolute/path` | Go directly using a full path |
| `ls` | List files in the current directory |
| `pwd` | Print your current location |
| `Tab` | Complete a filename or path — press twice to see all options |
| `ls -lh` | Long format with human-readable sizes |
| `ls -lhS` | Sorted by size — largest first |
| `ls -lt` | Sorted by modification time — newest first |
| `mkdir -p a/b/c` | Create a directory tree in one command |
| `cp SOURCE DEST` | Copy a file |

## Next

In the next exercise you will look inside files: `cat`, `head`, `tail`, and `wc` — and discover why running `cat` on the wrong file can flood your terminal.
