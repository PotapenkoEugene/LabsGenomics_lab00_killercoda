# Well done

## What you practiced

| Command | Purpose |
|---------|---------|
| `bwa-mem2 index ref.fna` | Build a searchable reference index |
| `bwa-mem2 mem ref.fna reads.fastq.gz > out.sam` | Align reads to the reference |
| `samtools flagstat file.sam` | Summarise mapping statistics |

## Next

Continue with **Lab 06 — Aggregate with MultiQC**: combine all QC outputs into one unified report.
