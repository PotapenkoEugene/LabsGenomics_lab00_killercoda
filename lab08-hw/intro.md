# Lab 08 Homework — Alternate Environment

The course server is unreachable — this VM is a drop-in replacement.

Your workspace is `~/labs/lab08/HW`. `fastqc`, `fastp`, `seqkit`, `megahit` are ready and active by default; `quast`, `busco`, `prokka` are installed as separate environments — activate each with `conda activate <name>`, exactly as the homework says. Environment is loading in the background — this one takes longer than the others (several tool environments to build).

Follow the **Lab 08 homework PDF on Moodle** for full task instructions. This scenario only gives you a place to run them and a way to get `answers.txt` back out.
