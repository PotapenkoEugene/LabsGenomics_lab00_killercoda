# Lab 03 Homework

Server's down — this VM replaces it. **No SSH needed**, you're already at the terminal.

**Workspace:** `~/labs/lab03/HW` (created, and already your working directory).

**Line 1 of `answers.txt`:** put your **real Tel-Hai ID**, not `student` — everyone on this VM logs in as `student`.

Open the **Lab 03 homework PDF (Moodle)** and work through Part 1 (NCBI, in your browser) and Tasks A–G here in the terminal, exactly as written.

---

## When you're done

Serve the workspace and grab your files:

```bash
~/view-reports.sh
```

Open: [click here]({{TRAFFIC_HOST1_8080}}) → right-click `answers.txt` → **Save Link As** to download it.

**Submit `answers.txt` to Moodle.** Grading is no longer done on the server.

Click **Check** once `answers.txt` (≥5 lines) and `h5n1.fna` (with `New_Segment_9`) exist.
