# Lab 03 Homework — Alternate Environment

The course server is unreachable — this VM is a drop-in replacement.

`wget`, `file`, `zcat`, `head`, `tail`, `wc`, `gunzip`, `vim` are all ready — environment is loading in the background.

> **Wait for the prompt to change** from root@ubuntu to student@ubuntu before typing anything. Killercoda can be laggy — if it's still root@ubuntu after a minute or two and nothing is happening, **reload the page and restart the scenario**.

![Wait for the prompt to change](./assets/wait-for-prompt.png)

Follow the **Lab 03 homework PDF on Moodle** for full task instructions. This scenario only gives you a place to run them and a way to get `answers.txt` back out.
