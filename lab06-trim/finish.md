# Well done

## What you practiced

| Command | Purpose |
|---------|---------|
| `fastp -i in.fastq.gz -o out.fastq.gz` | Trim adapters and low-quality bases |
| `fastqc file.fastq.gz` | Generate a per-sample quality report |

## Next

Continue with **Lab 06 — Align Reads**: index the SARS-CoV-2 reference and map your cleaned reads.
