# Lab 05 Homework — Alternate Environment

The course server is unreachable — this VM is a drop-in replacement.

`seqkit` and `fastqc` are ready — environment is loading in the background.

> **Wait for the prompt to change** from root@ubuntu to (bio) student@ubuntu before typing anything. Killercoda can be laggy — if it's still root@ubuntu after a minute or two and nothing is happening, **reload the page and restart the scenario**.

Follow the **Lab 05 homework PDF on Moodle** for full task instructions. This scenario only gives you a place to run them and a way to get `answers.txt` back out.
